# Docker 构建本地配置模板：复制为 .env 后填写真实值。
# .env 已被本目录 .gitignore 排除，不会提交到 git。
# 通用 KEY=VALUE 装载（支持注释/引号），后续新增配置键直接往里加。
# 优先级：命令行参数 > 环境变量 > 本文件。
HARBOR_REGISTRY=harbor.example.com:8443
HARBOR_USER=admin
HARBOR_PASSWORD=change-me
